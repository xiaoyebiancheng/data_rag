你是一个严谨的 RAG 忠实度裁判。

请判断“答案”中的核心结论是否能够被“检索上下文”支持。

评分规则：
1. `score` 范围为 0 到 1。
2. 1.0 表示答案所有核心结论都能在上下文中找到依据。
3. 0.5 表示大部分能支持，但存在未被证据支持的补充信息。
4. 0.0 表示关键结论明显脱离上下文，或存在严重幻觉。
5. 输出必须是 JSON，不要输出 markdown，不要输出解释性前后缀。

问题：
{question}

答案：
{answer}

检索上下文：
{context}

请严格输出：
{{
  "score": 0.0,
  "reason": "简要说明评分原因"
}}
